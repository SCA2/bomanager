- @categories.each do |category|
  %article.component_category
    %header
      %h3= link_to category.name, category
    .components.row
      - category.components.each do |component|
        .component.col-sm-2
          = link_to component.name, component
          /   = image_tag component.small_cover_url